Please continue development based on what we’ve built so far, keeping the same clean and minimal design, brand colors, and layout consistency.

1️⃣ Visitor Invite UI

Create a Visitor Invite screen that:

Displays a visit summary (visitor name, date/time, purpose).

Includes a placeholder for a QR code.

Provides two clear buttons: Accept and Reject.

When the visitor accepts or rejects:

Only update the local UI state (e.g., show “Invitation Accepted” or “Invitation Rejected” message).

No backend or shared state changes yet.

Allow this screen to be opened from the Role Selector, using a mock Visit ID for testing.

2️⃣ Receptionist UI

Extend the Receptionist area with two main screens:

Reception Dashboard:

Section showing a mock list of today’s visitors.

Add a simple search bar (frontend-only) to filter by name or phone number.

Walk-In Registration:

Basic form to capture walk-in visitor info (name, phone, host).

On submit, show a small confirmation UI (e.g., “Visitor registered successfully”).

Navigation flow:

Reception Role → Dashboard → Walk-In Registration → Back to Dashboard.

3️⃣ Security UI

Extend the Security area with two screens:

Gate Screen:

Input for entering or simulating a scanned code.

Result panel displays “Access Allowed” or “Access Denied” based on simple mock rules.

Lookup Screen:

Search UI for visitor name or phone.

Displays mock results with the ability to open a simple visitor details view.

Keep all logic mock/static only (no API or real data yet).

4️⃣ UX & Navigation

From Role Selector, enable the following navigation paths:

Visitor → Invite Screen

Receptionist → Dashboard → Walk-In Registration

Security → Gate → Lookup

Make sure all new screens:

Are mobile-friendly and responsive.

Maintain the same modern minimal UI and font system we already use.

Constraints:

Use mock/static data only for now.

No API integration or shared state — only local state as needed for UI behavior.

Keep design consistent with previous screens (spacing, typography, colors, rounded cards).

Goal:
Deliver fully functional UI journeys for Visitor, Receptionist, and Security roles, consistent with our current interface style and brand design.