Please fully test and debug the Buffet Staff Role to ensure all functionality and user flow work correctly and consistently across devices.

🧩 Issues to Fix

Hamburger Menu Crash

When the user taps the hamburger icon in the Buffet Staff role, the app currently crashes.

✅ Fix: ensure the drawer/navigation opens properly, same as in other roles (Receptionist / Security).

Check navigation links and permissions for the buffet role routes.

🧠 End-to-End User Flow (Functional Test)

Make sure the entire buffet flow is working and synchronized between roles:

🧾 1. Request Creation

When an Employee or Receptionist creates a buffet request:

It should automatically appear in the Buffet Admin dashboard.

The admin should be able to assign the buffet task to a staff member.

👨‍🍳 2. Buffet Staff Visibility

The assigned Buffet Staff must immediately see the new request on their dashboard.

Status should appear as Pending by default.

The request card should include all key info (visitor, company, guests, time, location, assigned to…).

🔄 3. Status Updates

When Buffet Staff change the status:

Pending → Preparing → Ready → Served

The change should automatically reflect in:

Buffet Admin dashboard.

Receptionist interface (if they monitor visitor requests).

🕒 4. Notifications

Staff should receive notifications for:

New buffet tasks assigned to them.

Any updates (time change or reassignment).

They should not receive irrelevant notifications (like visitor requests or unrelated roles).

⚙️ Technical Checks

Fix all navigation errors for the Buffet Staff drawer routes.

Ensure:

All buttons (Status update, View Details) are working.

Date and time pickers are functional and consistent.

UI matches the Receptionist role look and feel.

Test responsiveness on both web and mobile preview.

✅ Expected Result

Hamburger icon opens the correct navigation menu.

No crashes or navigation errors.

Buffet Staff see only their assigned tasks.

All status updates reflect live across roles.

Notifications show correctly per role.

Smooth, consistent experience across all devices.