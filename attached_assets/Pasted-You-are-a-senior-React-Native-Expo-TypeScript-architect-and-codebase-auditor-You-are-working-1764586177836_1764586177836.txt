You are a senior React Native + Expo + TypeScript architect and codebase auditor.

You are working in an existing project created in Replit. It is an Expo-based React Native app that targets:
- iOS
- Android
- Web (React Native Web / Expo Web)

This app is a Visitor Management System with multiple user roles (employee, manager, receptionist, building_admin, valet_admin, security, buffet_admin, etc.). The project already runs and has working screens, mock services, navigation, and theming.

VERY IMPORTANT CONSTRAINTS:
- Do NOT change any business logic or user flows.
- Do NOT change any visible UI behavior or screen layouts.
- Do NOT remove or break the current mock data flow.
- Preserve all existing route names and navigation structure (no renaming of routes).
- Prefer small, incremental refactors that improve structure and typing without breaking runtime behavior.
- After each major set of changes, ensure the TypeScript build and linting pass.

Your goal is to IMPLEMENT the following refactors and cleanups, based on an earlier audit, without hurting existing behavior.

==================================================
0. ORIENTATION
==================================================

First, scan the repo to understand the structure. You will see something similar to:

- App.tsx
- assets/
- components/
- constants/
  - theme.ts
- domain/
  - interfaces/IRepository.ts
  - models/Item.ts
- docs/
  - API_SPECIFICATION.md
- hooks/
- navigation/
  - DashboardContainer.tsx
  - MainTabNavigator.tsx
  - etc.
- screens/
  - Employee/*.tsx
  - Receptionist/*.tsx
  - ValetAdmin/*.tsx
  - Manager/*.tsx
  - BuildingAdmin/*.tsx
  - BuffetAdmin/*.tsx
  - Security/*.tsx
- services/
  - api.service.ts
  - user.service.ts
  - mock/
- types/
  - index.ts
  - vms.types.ts
  - *Navigation.types.ts (some already exist)
- utils/
  - i18n.ts
  - statusStyles.ts
  - etc.
- eslint.config.js
- tsconfig.json
- babel.config.js
- .replit
- scripts/build.js

Confirm this structure (or close to it) before you start refactoring.

==================================================
1. TYPESCRIPT: REMOVE `any` SAFELY AND ADD TYPECHECK SCRIPT
==================================================

Goal: Keep behavior identical, but tighten types and remove as many `any` usages as possible, especially in screens and navigation.

1.1. Add a typecheck script

- In package.json, add:
  - "typecheck": "tsc --noEmit"
  - "ci": "npm run lint && npm run check:format && npm run typecheck"

Make sure tsconfig.json already has "strict": true. If not, enable it, but do NOT introduce new breaking options beyond what’s already there.

1.2. Search for all `any` usages

- Focus on:
  - screens/…/*.tsx (especially BuffetAdmin, ValetAdmin, Employee, Manager, Receptionist, BuildingAdmin)
  - navigation/*.tsx
  - services/*.ts

1.3. Replace `any` for navigation props

- For each screen in screens/*, if you see something like:
  - function SomeScreen({ navigation, route }: any) { … }
  - or function SomeScreen({ navigation }: any)

Refactor to use typed React Navigation props:

- Create or extend navigation param lists in the `types` directory:

  - For each role, create a file if it does not exist:
    - types/employeeNavigation.types.ts
    - types/receptionistNavigation.types.ts
    - types/valetAdminNavigation.types.ts
    - types/managerNavigation.types.ts
    - types/buildingAdminNavigation.types.ts
    - types/buffetAdminNavigation.types.ts (already exists, just extend if needed)
    - types/securityNavigation.types.ts

- In each file, define a `*StackParamList` with route names that match the existing stack names and screen names. DO NOT change the route names; infer them from the existing navigation setup.

- In each screen, import:
  - NativeStackScreenProps (or the correct nav type for the navigator being used)
  - The corresponding *StackParamList

Example pattern:

  import { NativeStackScreenProps } from "@react-navigation/native-stack";
  import { EmployeeStackParamList } from "@/types/employeeNavigation.types";

  type Props = NativeStackScreenProps<EmployeeStackParamList, "VisitorRequests">;

  export default function VisitorRequestsScreen({ navigation, route }: Props) {
    ...
  }

- Do not change what the screen does, only how it is typed.

1.4. Replace `any` in event handlers

- Replace event-related `any` with proper React Native types:
  - Press handlers → GestureResponderEvent (from "react-native"), or reuse PressableProps["onPress"] when appropriate.
  - Scroll events → NativeSyntheticEvent<NativeScrollEvent>.

Example:

  import { GestureResponderEvent } from "react-native";

  const handlePress = (event: GestureResponderEvent) => {
    event.stopPropagation();
    ...
  };

1.5. Replace `theme: any` props

- Where you have props typed as `theme: any` or similar, replace with a proper type.
- Derive a Theme type from the existing theme definitions:
  - For example:
    - import { Colors } from "@/constants/theme";
    - type ThemeColors = typeof Colors.light;
  - Or use the return type from your useTheme hook:
    - type Theme = ReturnType<typeof useTheme>["theme"];

Use these types instead of `any`.

1.6. Other `any` cleanups

- For `ApiError.details?: any` in types/index.ts, change to:
  - details?: unknown;
  - or a more structured type if you can infer it from actual usage.
- For any other `any` usages, replace them with specific types derived from vms.types.ts and other existing types wherever possible.

1.7. Run and fix typecheck

- Run: npm run typecheck
- Fix any new type errors this refactor introduces, but do NOT rewrite business logic; only adjust types or small helper wrappers as necessary.

==================================================
2. NAVIGATION TYPING AND CONFIG CLEANUP (NO BEHAVIOR CHANGE)
==================================================

Goal: Keep the navigation behavior exactly the same but make it strongly typed and easier to maintain.

2.1. Centralize route param lists

- You already created *StackParamList types in Step 1.
- Ensure all navigators use these param lists:
  - In navigation/**/*.tsx files, connect createNativeStackNavigator<...>() with the appropriate param list.

2.2. Keep route names and structure IDENTICAL

- Do NOT rename any routes.
- Do NOT change which screens belong to which stack or how tabs are structured.
- Only update types and imports.

2.3. Optional code organization (ONLY if trivial)

- If and only if you can do it without changing behavior, you may:
  - Group related role-specific navigators into separate files, e.g. EmployeeNavigator, ReceptionistNavigator, etc.
- But if this starts to require significant rewiring, SKIP this step. Prioritize preserving current behavior.

==================================================
3. SERVICES & REPOSITORY LAYER (WRAPPING CURRENT MOCKS)
==================================================

Goal: Introduce a clean repository layer WITHOUT breaking existing mock behavior. For now, repositories should just wrap the existing mock services.

3.1. Keep existing mocks

- Do NOT delete or heavily rewrite services/mock/*.ts files.
- They should keep returning the same data as today.

3.2. Define repository interfaces in domain/interfaces

For example:

- domain/interfaces/VisitorRequestRepository.ts
- domain/interfaces/ValetRepository.ts
- domain/interfaces/BuffetRepository.ts
- domain/interfaces/NotificationRepository.ts

Each one should use existing types from types/vms.types.ts.

Example pattern for visitors (adjust methods based on how the screens currently query mock data):

  import { VisitorRequest } from "@/types/vms.types";

  export interface VisitorRequestRepository {
    getForEmployee(employeeId: string): Promise<VisitorRequest[]>;
    getForManager(managerId: string): Promise<VisitorRequest[]>;
    getPendingApprovals(managerId: string): Promise<VisitorRequest[]>;
    // add methods that match actual usage in screens
  }

3.3. Implement MOCK repositories that wrap existing mock functions

- In services/visitors/MockVisitorRequestRepository.ts:
  - Implement VisitorRequestRepository by calling the existing services/mock/visitorRequestState functions.
  - Do NOT change the mock data logic, only wrap it.

- Do the same for valet, buffet, etc., e.g.:
  - services/valet/MockValetRepository.ts
  - services/buffet/MockBuffetRepository.ts

3.4. Add a simple repository provider

- Create something like domain/RepositoryProvider.tsx:
  - Provide instances of the mock repositories (VisitorRequestRepository, ValetRepository, etc.) via React context.
  - For now, hardcode the mock implementations.
  - Do NOT integrate real HTTP yet, just prepare the pattern.

3.5. Incrementally switch screens to use repositories

- In screens that currently import from services/mock/*:
  - Replace direct imports with a hook from RepositoryProvider, e.g. useRepositories().
  - For example:

    const { visitorRequestRepository } = useRepositories();
    const data = await visitorRequestRepository.getForEmployee(employeeId);

- Make these replacements gradually, verifying that the behavior and data shown in the UI remain the same.

3.6. Keep ApiService as transport only

- Leave services/api.service.ts intact but consider moving it to services/http/apiClient.ts (only if it’s a trivial path update).
- For now, do NOT start using ApiService in repositories; just leave everything on mocks. This step is purely about structure.

==================================================
4. TYPES ORGANIZATION & IMPORT CLEANUP
==================================================

Goal: Make types easier to find and imports consistent without moving files into a new root or breaking anything.

4.1. Keep current type files but clarify roles

- types/index.ts:
  - Should hold app-level types: User, ApiResponse, ApiError, AppSettings, etc.
- types/vms.types.ts:
  - Holds domain types: VisitorRequest, ParkingSlot, ValetDriver, BuffetTask, Notification, RequestStatus, UserRole, etc.

Add comments at the top of these files explaining their purpose.

4.2. Navigation types

- Make sure all navigation param list types live in types/*Navigation.types.ts files and are imported everywhere instead of re-defining them ad hoc.

4.3. Use `@/` alias instead of long relative imports

- For any import that crosses folders and currently uses ../.., convert to `@/` alias, e.g.:

  - From:
    import { VisitorRequest } from "../types/vms.types";
  - To:
    import { VisitorRequest } from "@/types/vms.types";

- IMPORTANT:
  - Do NOT change relative imports for sibling files in the same folder (e.g. "./styles" is fine).
  - Only change cross-folder imports that point to root-level subfolders.

==================================================
5. STYLING & COMPONENT STANDARDIZATION (NO VISUAL CHANGE)
==================================================

Goal: Reduce inline styles and ensure consistent use of tokens (Spacing, BorderRadius, Typography, Colors) without changing the look.

5.1. Inline style cleanup

- In screens (especially dashboards and request lists), identify repeated inline style objects:
  - e.g., row containers, filter chips, cards, status pills.
- Move these repeated styles into StyleSheet.create blocks.
- If you see the same pattern in multiple screens (e.g., chips / pills), consider extracting a small shared component in components/:
  - e.g., components/Chip.tsx or components/FilterPill.tsx.

5.2. Use tokens for repeated dimensions

- If you see the same width/height values (like 80, 44, etc.) used in multiple places, create a Sizing constant in constants/theme.ts or a new constants/sizing.ts and use those instead.
- Do NOT alter the actual numeric values at this time; simply centralize them.

5.3. Preserve theming pattern

- Always use Colors, Spacing, BorderRadius, and Typography from constants/theme.ts.
- Prefer ThemedView / ThemedText over plain View / Text in screens where theming is already used.

==================================================
6. ACCESSIBILITY & I18N PREP (LIGHT TOUCH)
==================================================

Goal: Add basic accessibility props and start preparing for i18n, without changing visible UI or actual wording.

6.1. Accessibility props

- For all interactive elements (Pressable, Button components, icon-only buttons), add:
  - accessibilityRole="button" (or appropriate role)
  - accessibilityLabel with a clear English label
- Do NOT change text labels shown in UI; this is only for screen readers.

6.2. i18n scaffolding (no actual translations yet)

- In utils/i18n.ts, if not present already, add the ability to map string keys to labels for "en" and "ar" (even if you initially just mirror the English strings).
- Create a tiny string dictionary and a useStrings() hook that returns the labels for a given language.
- Do NOT replace all hard-coded strings yet.
  - Only set up the infrastructure and perhaps move a few obvious global labels (like "Sign In") as examples, without changing what is displayed.

==================================================
7. REPO TOOLING & FORMATTING
==================================================

Goal: Ensure formatting and linting are consistent and easy to run.

7.1. Prettier config

- Add a .prettierrc at the project root with reasonable defaults, for example:

  {
    "singleQuote": false,
    "trailingComma": "all",
    "printWidth": 100,
    "tabWidth": 2,
    "semi": true
  }

- If not present, add .prettierignore to exclude:
  - node_modules
  - dist
  - web-build
  - .expo
  - .config
  - etc.

7.2. ESLint / import order

- Ensure eslint.config.js is correctly configured for TypeScript, React, and React Native (it likely extends eslint-config-expo/flat).
- If it’s easy, add basic import ordering (React, then third-party, then "@/...", then relative imports). Do NOT aggressively reformat the entire repo; just keep it consistent within modified files.

7.3. CI script

- Ensure package.json has:
  - "lint"
  - "format"
  - "check:format"
  - "typecheck"
  - "ci": "npm run lint && npm run check:format && npm run typecheck"

==================================================
8. VALIDATION – ENSURE NOTHING IS BROKEN
==================================================

After you finish the changes above:

- Run:
  - npm run lint
  - npm run check:format
  - npm run typecheck

Fix issues until all commands succeed.

If possible in this environment:
- Build or run the app (for web and at least one native target) to ensure there are no runtime errors and that navigation and screens still behave exactly as before.
- Verify that:
  - All previous screens still open.
  - All previous flows still reach their destination.
  - No TypeScript errors.
  - No build errors.

DO NOT add new features or change any business logic. This task is solely about:
- better typing,
- safer navigation,
- clearer services / repository structure,
- more consistent styling,
- basic accessibility hooks,
- and repo hygiene.

When in doubt, pick the safest, least-invasive change that satisfies these goals.
