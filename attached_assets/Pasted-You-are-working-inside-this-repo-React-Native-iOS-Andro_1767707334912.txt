You are working inside this repo (React Native: iOS/Android + Web). We are doing a full BRAND REFRESH.

GOAL
1) First detect the CURRENT/OLD brand colors + typography used in the codebase (tokens + hardcoded).
2) Replace/migrate them to the NEW Dallah Albaraka brand guide below (colors + fonts).
3) Centralize tokens so future changes are easy (no scattered hex codes).
4) Produce a short migration report so the team can review.

NEW BRAND GUIDE (SOURCE OF TRUTH)

COLORS
Primary:
- Dallah Orange: #F58423 (RGB 245,132,35)
  Tints (mix with white):
  - 80%: #F79D4F
  - 60%: #F9B57B
  - 40%: #FBCEA7
  - 20%: #FDE6D3

- Dallah Green: #009933 (RGB 0,153,51)
  Tints:
  - 80%: #33AD5C
  - 60%: #66C285
  - 40%: #99D6AD
  - 20%: #CCEBD6

Secondary:
- Dallah Grey: #282829 (RGB 40,40,41)
  Tints:
  - 80%: #535354
  - 60%: #7E7E7F
  - 40%: #A9A9A9
  - 20%: #D4D4D4

TYPOGRAPHY
Latin (English): “FS Albert Pro”
- ExtraBold = Headlines
- Bold = Subheadlines
- Regular = Body
- Light = Exceptional cases

Arabic: “FS Albert Arabic Web”
- ExtraBold = Headlines
- Bold = Subheadlines
- Regular = Body

IMPORTANT: If font files are not present in repo, do NOT invent binaries.
- Implement the plumbing with clear placeholders:
  - Web: @font-face pointing to /public/fonts (or equivalent) and fallbacks if files missing
  - RN: expo-font/react-native linking setup with fallbacks if files missing
- Add a note in docs about where to place font files and expected filenames.

WHAT TO DO (STEP-BY-STEP)

A) INVENTORY (do this first)
1) Scan the repo for:
   - existing design token files (colors/typography/theme)
   - tailwind config (if used), global CSS, styled-components theme, MUI/Ant theme, etc.
   - hardcoded hex colors (#xxxxxx), rgb(), hsl(), and “brand*” named constants.
2) Create docs/BRAND_MIGRATION_REPORT.md with:
   - “Old tokens found” (names + values + file paths)
   - “Hardcoded colors found” (top offenders + file paths)
   - “Old typography/font families found” (and where)
   - A mapping table: OLD → NEW (best semantic match)
   - Any risks (contrast, dark mode, etc.)

B) CREATE / UPDATE CENTRAL TOKENS
3) Create or update a single source of truth for tokens:
   - Colors: define base + tints + semantic roles (primary, success, text, bg, border, muted, etc.)
   - Typography: font families + weight mapping + shared sizing scale if you already have one.
   Suggested structure (adapt to repo):
   - src/theme/tokens/colors.ts (or .js)
   - src/theme/tokens/typography.ts
   - src/theme/index.ts exports a theme object used by both RN + Web (if architecture allows)

Rules:
- Remove scattered hex codes in components: replace with tokens or CSS variables.
- Prefer semantic names in UI (e.g., colorPrimary, colorSuccess, textPrimary) but ensure they resolve to Dallah palette.

C) APPLY MIGRATION ACROSS APP
4) Replace old colors with new tokens:
   - Buttons, links, badges, alerts, focus rings, icons
   - Gradients/overlays if any: re-map to Orange/Green/Grey scheme
   - Status colors: use Green for success, Orange for warning/primary accents (unless existing semantics dictate otherwise)
5) Typography:
   - Set default Latin font stack to: "FS Albert Pro", system-ui, -apple-system, Segoe UI, Roboto, Arial
   - For Arabic content (lang="ar" on web OR i18n/RTL locales on RN), use: "FS Albert Arabic Web" first.
   - Ensure font-weights map correctly (ExtraBold/Bold/Regular/Light).
6) RTL/Arabic support:
   - Web: apply Arabic font via [lang="ar"] selectors or a .rtl/.lang-ar wrapper.
   - RN: keep existing RTL handling; ensure Arabic screens pick Arabic font family token.

D) WEB + RN FONT PLUMBING (NO BINARY FILES)
7) Web:
   - Add fonts CSS (e.g., src/styles/fonts.css or app/globals.css):
     - @font-face entries referencing /public/fonts/FSAlbertPro-*.woff2 and /public/fonts/FSAlbertArabicWeb-*.woff2
     - if files missing, keep fallbacks so build still works.
8) RN:
   - If Expo: use expo-font to load from assets/fonts/* and wire into theme.
   - If bare RN: document the linking steps and keep fallback until assets exist.
9) Add docs/FONTS_SETUP.md explaining:
   - which font files are required
   - filenames expected
   - where to put them (web + mobile)

E) ACCEPTANCE CHECKS
10) Ensure:
   - No old brand hex codes remain (except in historical docs if needed).
   - App builds for web and mobile.
   - Visual smoke check: primary button, secondary button, header text, Arabic screen sample (if exists).
11) If you find conflicts (e.g., current brand palette includes blues used for links), choose the closest semantic replacement using Grey/Orange/Green and note it in BRAND_MIGRATION_REPORT.md.

OUTPUTS REQUIRED
- Updated tokens + applied across UI
- docs/BRAND_MIGRATION_REPORT.md
- docs/FONTS_SETUP.md
- (Optional) docs/BRAND_GUIDE.md summarizing palette + typography for devs/designers

Now proceed: do the inventory first, write the report, then implement tokens and migrate usage across the project.
